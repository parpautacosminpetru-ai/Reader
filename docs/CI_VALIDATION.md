# CI Validation

This branch exists to validate the Reader Android foundation through a pull-request GitHub Actions run before feature workers start.

Validation gate:

- unit tests pass;
- debug APK assembles;
- APK artifact is uploaded;
- no critical build errors remain.
